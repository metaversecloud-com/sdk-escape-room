apiVersion: v1
kind: Service
metadata:
  name: esc-room0
  labels:
    app.kubernetes.io/name: esc-room0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: esc-room0
  ports:
    - name: http
      port: 80
      targetPort: http
